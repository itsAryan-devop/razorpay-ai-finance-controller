# Secrets — NEVER commit
.env
*.key
secrets/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# Data — keep generators in git, not large generated artifacts
data/generated/
*.parquet

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Downloaded third-party binary (fetched per tools/README.md instructions, checksum-
# verified against the official release — not committed; tools/README.md itself IS tracked)
tools/razorpay-mcp-server/

# Local-only: editor/tooling config and internal planning journal (not part of the submission)
.claude/
NOTES.md
